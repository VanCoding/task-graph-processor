{
  "name": "watch-task-protocol-typescript",
  "version": "0.2.0",
  "description": "",
  "type": "module",
  "bin": {
    "tsc-watch": "dist/index.js"
  },
  "scripts": {
    "build": "wireit",
    "clean": "rm -rf dist",
    "test": "NODE_OPTIONS='--loader ts-node/esm --no-warnings' jest src/**",
    "deploy": "pnpm run clean && pnpm run build && pnpm run test && pnpm publish"
  },
  "wireit": {
    "build": {
      "command": "tsc",
      "dependencies": [
        "../../watch-task-protocol:build"
      ]
    }
  },
  "keywords": [],
  "author": "Patrik Stutz",
  "license": "MIT",
  "dependencies": {
    "watch-task-protocol": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^4.9.4"
  },
  "jest": {
    "runner": "jest-light-runner",
    "testTimeout": 1000
  },
  "devDependencies": {
    "@types/jest": "^29.2.4",
    "@types/node": "^18.11.13",
    "jest": "^29.3.1",
    "jest-light-runner": "^0.4.1",
    "testtriple": "^2.2.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
