{
  "name": "watch-task-protocol",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    "./*": "./dist/*"
  },
  "scripts": {
    "test": "NODE_OPTIONS='--loader ts-node/esm --no-warnings' jest src/**",
    "build": "tsc",
    "clean": "rm -rf dist",
    "deploy": "pnpm run clean && pnpm run build && pnpm run test && pnpm publish"
  },
  "dependencies": {
    "npm-run-path": "^5.1.0",
    "split2": "^4.1.0"
  },
  "jest": {
    "runner": "jest-light-runner",
    "testTimeout": 1000
  },
  "devDependencies": {
    "@types/jest": "^29.2.4",
    "@types/node": "^18.11.13",
    "@types/split2": "^3.2.1",
    "jest": "^29.3.1",
    "jest-light-runner": "^0.4.1",
    "testtriple": "^2.2.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
